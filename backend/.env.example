# Supabase: use the CONNECTION POOLER string (port 6543) for runtime queries.
# Append ?pgbouncer=true   e.g. postgresql://postgres.<ref>:<pw>@aws-0-<region>.pooler.supabase.com:6543/postgres?pgbouncer=true
DATABASE_URL=""
# Supabase: use the DIRECT connection string (port 5432) for prisma migrate/db push.
# e.g. postgresql://postgres.<ref>:<pw>@aws-0-<region>.pooler.supabase.com:5432/postgres
DIRECT_URL=""
CLERK_SECRET_KEY=""
CORS_ORIGINS="http://localhost:3000,http://localhost:5173,http://localhost:5174,https://admin.runwayrefinedbyalek.com"
PORT=3001
RESEND_API_KEY=""
RESEND_FROM_EMAIL="Runway Refined <onboarding@resend.dev>"
RESEND_TO_EMAIL=""
# Comma-separated emails allowed to call /api/admin (must match admin VITE_ADMIN_ALLOWED_EMAILS)
ADMIN_ALLOWED_EMAILS="runwayrefinedofficial@gmail.com"
SITE_URL="http://localhost:5173"
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""
